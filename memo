プレスリリース本文は tr "０-９ー－" "0-9--" した後に extract_address.pl を適用する
-> やはり変換すると座標を合わせるのが非常に大変なので、元のデータのまま抽出して、完全一致で座標を取得することにする
-> 丁目までは基本的に取られて来るだろう
* 変なものが付いてきた場合は、その後のgeoencodingで下位のクラスに分類されるので排除できる


企業名は
1) 形態素に分解
2) 曖昧な形態素を除去
3) 残った形態素が、形態素単位でどれだけ完全一致するか計上
3-1) それぞれを形態素に分解して、集合として比較
3-2) 企業リストの方だけを形態素に分解して、文章に包含され、かつその両端が文章での形態素の境界である場合に一致とする



スコアリング

住所
なし
都道府県だけ
市区町村だけ
丁目まで

企業名
なし
